services:
  postgres-db:
    image: postgres:latest
    environment:
      POSTGRES_DB: test-containers-db
      POSTGRES_USER: dbuser
      POSTGRES_PASSWORD: dbpassword
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data`
